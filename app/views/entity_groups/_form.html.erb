<%= form_with(model: @entity, url: group_entities_path, method: :post) do |form| %>
  <% if entity_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(entity_group.errors.count, "error") %> prohibited this entity_group from being saved:</h2>

      <ul>
        <% entity_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>